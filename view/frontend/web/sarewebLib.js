require([
    'jquery',
    'sarewebLib','domReady!'
], function($) {

   /*
   // Uncomment for non-AJAX add/remove from cart
   $.ajax({
        showLoader: true,
        url: '/sarehub/index/index',
        type: "GET",
        dataType: 'json'
    }).done(function (data) {
        for (var key in data) {
            let productData = data;
            sareX_core.execute(10, productData);
        }
    }); */
});
